> **DEVELOPMENT ONLY — NOT FOR PRODUCTION USE.** Release `0.3.120` is mutable; verify every downloaded SHA-256 against committed metadata before installation or execution.

This is the permanent mutable development-artifact warehouse. Canonical metadata and digests are in [`metadata/releases/0.3.120.json`](https://github.com/effectstream/binaries/blob/main/metadata/releases/0.3.120.json).

Assets are append-only. Existing names and bytes are never overwritten, deleted, replaced, or `--clobber`ed. A corrected or Developer-ID-signed artifact uses a new family-conforming name/version and a new catalog row.

macOS entries explicitly state their distribution-signing status. `UNSIGNED_DEVELOPMENT_ONLY` means no Developer ID; a linker/ad-hoc signature does not make an artifact production-ready.
